<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.unipet.mapper.UserMapper">

    <!-- 사용자 일반 로그인 -->
    <select id="selectUser" parameterType="hashmap" resultType="com.example.unipet.model.User">
        SELECT
            USER_ID AS userId,
            USER_PWD AS pwd,
            USER_NAME AS userName,
            NICKNAME AS nickname,
            PHONE,
            EMAIL,
            USER_ADDR AS userAddr,
            FULL_ADDR AS fullAddr,
            ZIPCODE,
            SOCIALTYPE AS socialType,
            CDATE AS cdate,
            USER_STATUS AS userStatus
        FROM users
        WHERE USER_ID = #{userId}
          AND USER_PWD = #{pwd}
    </select>

    <!-- 사업자 로그인 -->
    <select id="selectStoreUser" parameterType="hashmap" resultType="com.example.unipet.model.User">
        SELECT
            S_USER_ID AS sUserId,
            S_USER_PWD AS sUserPwd,
            CEO_NAME AS ceoName
        FROM store_user
        WHERE S_USER_ID = #{userId}
          AND S_USER_PWD = #{pwd}
    </select>

    <!-- 아이디 중복 체크 -->
    <select id="checkUser" parameterType="hashmap" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE USER_ID = #{userId}
    </select>

    <!-- 일반 회원가입 -->
    <insert id="insertUser" parameterType="hashmap">
        INSERT INTO users (
            USER_ID,
            USER_PWD,
            EMAIL,
            USER_NAME,
            NICKNAME,
            PHONE,
            USER_ADDR,
            FULL_ADDR,
            ZIPCODE,
            SOCIALTYPE,
            CDATE,
            USER_STATUS
        ) VALUES (
            #{userId},
            #{pwd},
            #{email},
            #{userName},
            #{nickname},
            #{phone},
            #{userAddr},
            #{fullAddr},
            #{zipcode},
            #{socialType},
            NOW(),
            'NOR'
        )
    </insert>

    <!-- 소셜 로그인용 조회 -->
    <select id="selectUserByUserId" parameterType="hashmap" resultType="com.example.unipet.model.User">
        SELECT
            USER_ID AS userId,
            USER_NAME AS userName,
            NICKNAME AS nickname,
            PHONE,
            EMAIL,
            USER_ADDR AS userAddr,
            FULL_ADDR AS fullAddr,
            ZIPCODE,
            SOCIALTYPE AS socialType,
            CDATE AS cdate,
            USER_STATUS AS userStatus
        FROM users
        WHERE USER_ID = #{userId}
    </select>

    <!-- 소셜 회원 자동 등록 -->
    <insert id="insertSocialUser" parameterType="hashmap">
        INSERT INTO users (
            USER_ID,
            USER_PWD,
            EMAIL,
            USER_NAME,
            NICKNAME,
            PHONE,
            USER_ADDR,
            FULL_ADDR,
            ZIPCODE,
            SOCIALTYPE,
            CDATE,
            USER_STATUS
        ) VALUES (
            #{userId},
            '',
            #{email},
            #{userName},
            #{nickname},
            '',
            '',
            '',
            '',
            #{socialType},
            NOW(),
            'NOR'
        )
    </insert>

</mapper>