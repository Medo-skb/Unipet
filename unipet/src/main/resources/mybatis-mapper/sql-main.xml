<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.example.unipet.mapper.MainMapper">
	<!-- 예약 많은 업체 -->
	<select id="selectPopularStoreList" resultType="com.example.unipet.model.Main">
	    SELECT 
		    s.STORE_NO AS storeNo,
		    s.STORE_NAME AS storeName,
		    s.S_CATEGORY AS sCategory,
		    c.KOR AS sCategoryName,
		    s.S_ADDR AS sAddr,
		    pm.MENU_NAME AS popularMenuName
		FROM STORE s
		JOIN RESERVATION r 
		    ON s.STORE_NO = r.STORE_NO
		LEFT JOIN CODE c 
		    ON s.S_CATEGORY = c.VALUE
		    AND c.TYPE = 'S_CATEGORY'
		LEFT JOIN (
		    SELECT STORE_NO, MENU_NAME
		    FROM (
		        SELECT 
		            m.STORE_NO,
		            m.MENU_NAME,
		            ROW_NUMBER() OVER (
		                PARTITION BY m.STORE_NO
		                ORDER BY COUNT(*) DESC, m.MENU_NO ASC
		            ) AS rn
		        FROM RESERVATION r
		        JOIN STORE_MENU m
		            ON r.MENU_NO = m.MENU_NO
		        GROUP BY m.STORE_NO, m.MENU_NO, m.MENU_NAME
		    ) x
		    WHERE rn = 1
		) pm
		    ON s.STORE_NO = pm.STORE_NO
		GROUP BY 
		    s.STORE_NO, 
		    s.STORE_NAME, 
		    s.S_CATEGORY,
		    c.KOR,
		    s.S_ADDR,
		    pm.MENU_NAME
		ORDER BY COUNT(r.RSV_NO) DESC
		LIMIT 4;
	</select>

	<!-- 사람들이 많이 찜한 인기상품 -->
	<select id="selectPopularProductList" resultType="com.example.unipet.model.Main">
	    SELECT
	        p.PRODUCT_NO AS productNo,
	        p.PRODUCT_NAME AS productName,
	        p.PRODUCT_PRICE AS productPrice,
	        p.I_SUB_NO AS iSubNo
	    FROM PRODUCT p
	    JOIN WISH w
	        ON p.PRODUCT_NO = w.PRODUCT_NO
	    WHERE p.PRODUCT_STATUS = 'Y'
	    GROUP BY
	        p.PRODUCT_NO,
	        p.PRODUCT_NAME,
	        p.PRODUCT_PRICE,
	        p.I_SUB_NO
	    ORDER BY COUNT(w.WISH_NO) DESC
	    LIMIT 4
	</select>
	
</mapper>